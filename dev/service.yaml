apiVersion: v1
kind: Service
metadata: 
  name: afstemning
  namespace: afstemningdev
  labels:
    app: afstemning
spec:
  type: NodePort
  ports:
    - port: 30000
      targetPort: 8000
      nodePort: 30000
  selector:
    app: afstemning